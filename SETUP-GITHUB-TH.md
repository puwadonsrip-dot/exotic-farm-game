# เปิดโปรเจกต์นี้ที่เครื่องอื่น

โปรเจกต์อยู่บน GitHub เรียบร้อยแล้ว ที่

```
https://github.com/puwadonsrip-dot/exotic-farm-game
```

เป็น repo แบบ Public — ล็อกอินบัญชีเดิมที่เครื่องไหนก็ดึงงานลงมาเปิดได้เลย

---

## ⚠️ อ่านก่อน — เรื่องเวลา

Unity ต้องสร้างโฟลเดอร์ `Library/` ใหม่ทั้งหมดตอนเปิดครั้งแรก
ระหว่างนั้นหน้าจอจะเหมือนค้าง กด Play ไม่ได้ ทำอะไรไม่ได้เลย

`Library/` ไม่ได้อยู่บน GitHub โดยตั้งใจ (มันหลายกิกะไบต์ และ Unity สร้างใหม่เองได้)
การรอรอบแรกเลยเลี่ยงไม่ได้

**ทดสอบจริงแล้ว: ใช้เวลาประมาณ 5 นาที** (16 ก.ย. 2569 — โคลนลง `C:\test-clone` แล้วเปิด)

แต่นั่นคือเครื่องที่บ้าน ถ้าเครื่องที่งานเป็นฮาร์ดดิสก์จานหมุนแทน SSD หรือสเปกต่ำกว่า
อาจนานกว่านี้หลายเท่า **เผื่อเวลาไว้ 30 นาที** จะได้ไม่ลุ้น

ถ้าเลือกได้ **ไปซ้อมเปิดล่วงหน้าคนละวัน** ดีที่สุด เพราะเปิดครั้งแรกเสร็จแล้ว
ครั้งต่อไปเปิดเร็วปกติ (ไม่ถึงนาที)

---

## สิ่งที่ต้องมีที่เครื่องปลายทาง

| ต้องมี | หมายเหตุ |
|---|---|
| Unity **6000.3.19f1** | ต้องตรงเวอร์ชัน เช็กที่ Unity Hub → Installs |
| Unity Hub | มาคู่กับ Unity อยู่แล้ว |
| GitHub Desktop | https://desktop.github.com |
| พื้นที่ว่าง | เผื่อสัก **10 GB** เพราะ `Library/` ที่สร้างใหม่กินเยอะ |
| เน็ต | ใช้ตอนโหลดครั้งแรกเท่านั้น |

---

## 3 ขั้นตอน

### 1. ดึงงานลงมา

เปิด **GitHub Desktop** → `Sign in to GitHub.com` → ล็อกอินบัญชีเดิม

`File` → `Clone repository` → แท็บ **GitHub.com** → เลือก **`exotic-farm-game`** → กด **Clone**

> ถ้าไม่เห็นในรายการ กดแท็บ **URL** แล้ววาง `puwadonsrip-dot/exotic-farm-game`

### 2. เอาเข้า Unity

Unity Hub → `Add` → `Add project from disk` → ชี้ไปที่โฟลเดอร์ที่เพิ่งโคลนมา

### 3. เปิด แล้วรอ

กดเปิดโปรเจกต์ จะขึ้นแถบ `Importing assets` วิ่งไปเรื่อยๆ **อย่าปิด อย่ากดอะไร**

พอเข้าหน้า Editor ได้แล้ว เปิดซีนหลัก **`Assets/project.unity`** → กด **Play**

---

## ถ้า UI หรือระบบบางอย่างหาย

ไปที่ **Tools → Farming → ติดตั้งระบบ Farming** กดหนึ่งครั้ง แล้วเซฟซีน (`Ctrl+S`)

---

## แผนสำรองสำหรับวันพรีเซน

อย่าพึ่งทางเดียว ถ้าเน็ตล่มหรือโหลดไม่ทันจะจบเลย
**ทำไฟล์เกมสำเร็จรูปติด USB ไปด้วย** ใช้เวลาไม่กี่นาที ทำที่เครื่องตัวเอง

1. ใน Unity → `File` → `Build Profiles`
2. เลือก **Windows** → กด **Build**
3. เลือกโฟลเดอร์ว่าง เช่น `ExoticFarm_Build`
4. รอเสร็จ แล้วก๊อบทั้งโฟลเดอร์ลง USB

ที่งานเสียบ USB ดับเบิลคลิก `.exe` เล่นได้ทันที ไม่ต้องมี Unity ไม่ต้องมีเน็ต

---

## ใช้งานประจำวัน (ทำงานสลับสองเครื่อง)

**ก่อนลุกจากเครื่อง**

1. **ปิด Unity ก่อน**
2. GitHub Desktop → พิมพ์สรุปสั้นๆ ว่าทำอะไร → **Commit to main** → **Push origin**

**ก่อนเริ่มทำงานที่อีกเครื่อง**

1. เปิด GitHub Desktop **ก่อนเปิด Unity**
2. กด **Fetch origin** → ถ้ามีของใหม่กด **Pull origin**
3. ค่อยเปิด Unity

> กฎเดียวที่ต้องจำ: **push ก่อนลุก, pull ก่อนเริ่ม**
> ถ้าลืมแล้วไปแก้ไฟล์เดียวกันสองเครื่อง จะเกิด conflict ซึ่งแก้ยากมากในไฟล์ซีนของ Unity

---

## ปัญหาที่เจอบ่อย

**เปิดที่เครื่องใหม่แล้วซีนว่าง / ของหายหมด**
→ ไฟล์ `.meta` ไม่ครบ ลองเช็กบน GitHub ว่ามี `.meta` อยู่ข้างๆ ไฟล์ภาพไหม

**Unity ขอ upgrade โปรเจกต์ตอนเปิด**
→ เวอร์ชัน Unity ไม่ตรง **อย่ากด upgrade** ให้ไปติดตั้ง `6000.3.19f1` ก่อน

**เมนู Tools → Farming ไม่ขึ้น**
→ โค้ดยังคอมไพล์ไม่เสร็จ หรือมี error ดูที่หน้าต่าง Console

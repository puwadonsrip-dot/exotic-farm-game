# วิธีเอาโปรเจกต์ขึ้น GitHub แล้วไปเปิดที่เครื่องอื่น

ทำครั้งเดียวจบ ไม่ต้องพิมพ์คำสั่งอะไรเลย ใช้โปรแกรม **GitHub Desktop** ที่มีหน้าจอกดๆ

---

## เตรียมพร้อมแล้ว

ไฟล์สองอันนี้ถูกใส่ไว้ในโปรเจกต์ให้แล้ว ไม่ต้องแตะต้อง

- **`.gitignore`** — บอก Git ว่าไม่ต้องอัปโฟลเดอร์ `Library/`, `Temp/`, `Logs/` และไฟล์ `.csproj`
  พวกนี้ Unity สร้างใหม่เองได้ และรวมกันหลายกิกะไบต์ ถ้าอัปไปด้วยจะช้ามากและเปลืองพื้นที่เปล่าๆ
- **`.gitattributes`** — กันไฟล์ซีนพังตอนสลับไปมาระหว่าง Windows กับ Mac

---

## ขั้นที่ 1 — สมัคร GitHub

ถ้ายังไม่มีบัญชี สมัครฟรีที่ https://github.com/signup

---

## ขั้นที่ 2 — ติดตั้ง GitHub Desktop

โหลดจาก https://desktop.github.com แล้วติดตั้งตามปกติ

เปิดโปรแกรมครั้งแรกมันจะให้ **Sign in to GitHub.com** — ล็อกอินด้วยบัญชีที่สมัครไว้
(หน้าล็อกอินจะเด้งไปที่เว็บเบราว์เซอร์ เป็นเรื่องปกติ)

จากนั้นมันจะถามชื่อกับอีเมลสำหรับเซ็นกำกับงาน ใส่ไปตามจริง กด Finish

---

## ขั้นที่ 3 — เพิ่มโปรเจกต์เข้าไป

1. ในหน้าแรกกด **Add an Existing Repository from your Hard Drive...**
   (หรือเมนูบนสุด `File → Add local repository`)
2. กด **Choose...** แล้วเลือกโฟลเดอร์นี้

   ```
   C:\Users\UsEr\Downloads\project (1)\project
   ```

   > ต้องเลือกโฟลเดอร์ `project` ตัวในสุดที่มี `Assets`, `Packages`, `ProjectSettings` อยู่ข้างใน
   > ไม่ใช่ `project (1)` ข้างนอก

3. มันจะขึ้นข้อความสีแดงว่า **"This directory does not appear to be a Git repository"**
   ให้กดคำว่า **create a repository** ที่เป็นลิงก์ในข้อความนั้น

4. หน้าต่างใหม่จะเด้งขึ้นมา กรอกแบบนี้

   | ช่อง | ใส่อะไร |
   |---|---|
   | Name | `exotic-farm` |
   | Description | `เกมฟาร์มพิกเซล 2D ทำด้วย Unity` |
   | Git ignore | เลือก **None** — เรามีไฟล์ `.gitignore` ของตัวเองอยู่แล้ว |
   | License | **None** — เพราะอาร์ตบางส่วนไม่ได้วาดเอง เราจึงไม่มีสิทธิ์ยกให้คนอื่นใช้ต่อ |

5. กด **Create repository**

รอสักครู่ มันจะไล่อ่านไฟล์ทั้งโปรเจกต์ ถ้าเห็นรายการไฟล์ขึ้นมาหลักพันไฟล์ = ปกติ

> **เช็กให้ชัวร์:** ในรายการไฟล์ทางซ้าย ต้อง **ไม่มี** อะไรที่ขึ้นต้นด้วย `Library/` หรือ `Temp/`
> ถ้ามี แปลว่า `.gitignore` ไม่ได้ทำงาน — ทักมาบอกได้

---

## ขั้นที่ 4 — คอมมิตครั้งแรก

ที่มุมล่างซ้ายจะมีช่องให้พิมพ์

- ช่องบน (Summary) พิมพ์ว่า `เริ่มต้นโปรเจกต์ Exotic Farm`
- กดปุ่มฟ้า **Commit to main**

---

## ขั้นที่ 5 — อัปขึ้นเว็บ

1. กดปุ่ม **Publish repository** ที่แถบบน
2. หน้าต่างจะเด้งมา — **เอาเครื่องหมายถูกหน้า "Keep this code private" ออก** ถ้าอยากให้เป็น Public
3. กด **Publish repository**

รอจนแถบวิ่งเสร็จ (ครั้งแรกอาจนานหน่อย เพราะไฟล์ภาพเยอะ)

เสร็จแล้ว กด **Repository → View on GitHub** เพื่อเปิดดูบนเว็บ
ลิงก์จะหน้าตาประมาณ `https://github.com/ชื่อคุณ/exotic-farm`

---

## ขั้นที่ 6 — ไปเปิดที่เครื่องอื่น (ตอนพรีเซน)

> ### ⚠️ อ่านก่อน — จุดที่พังบ่อยที่สุด
>
> **ห้ามโคลนโปรเจกต์วันพรีเซน** เด็ดขาด
>
> Unity ต้องสร้างโฟลเดอร์ `Library/` ใหม่ทั้งหมดตอนเปิดครั้งแรก ซึ่ง **กินเวลา 10–40 นาที**
> ระหว่างนั้นหน้าจอจะเหมือนค้าง กด Play ไม่ได้ ทำอะไรไม่ได้เลย
>
> **ต้องไปถึงก่อนอย่างน้อย 1 ชั่วโมง** หรือถ้าเป็นไปได้ ไปซ้อมเปิดล่วงหน้าคนละวัน

### สิ่งที่ต้องมีที่เครื่องปลายทาง

| ต้องมี | หมายเหตุ |
|---|---|
| Unity **6000.3.19f1** | ต้องตรงเวอร์ชัน ถ้าเปิดด้วยเวอร์ชันอื่นอาจมีปัญหา |
| Unity Hub | มาคู่กับ Unity อยู่แล้ว |
| GitHub Desktop | ใช้ล็อกอินและดึงงาน |
| พื้นที่ว่าง | เผื่อไว้สัก **10 GB** เพราะ `Library/` ที่สร้างใหม่กินเยอะ |
| เน็ต | ใช้ตอนโหลดครั้งแรกเท่านั้น |

### ขั้นตอน

1. **ติดตั้ง Unity ให้ตรงเวอร์ชันก่อน**
   Unity Hub → `Installs` → `Install Editor` → ถ้าไม่เจอ `6000.3.19f1` ในรายการ
   กดแท็บ **Archive** → `download archive` → หาเวอร์ชันนี้ในเว็บแล้วกด Install
   (ขั้นนี้นานสุด อาจครึ่งชั่วโมง — ทำล่วงหน้าที่บ้านได้ถ้ารู้ว่าจะใช้เครื่องไหน)

2. **ติดตั้ง GitHub Desktop แล้วล็อกอิน**
   https://desktop.github.com → เปิดโปรแกรม → `Sign in to GitHub.com` → ใส่บัญชีคุณ

3. **ดึงงานลงมา**
   `File → Clone repository` → แท็บ **GitHub.com** → เลือก `exotic-farm` → กด **Clone**
   (ถ้าไม่เห็นในรายการ กดแท็บ URL แล้วพิมพ์ `ชื่อคุณ/exotic-farm`)

4. **เพิ่มเข้า Unity Hub**
   Unity Hub → `Add` → `Add project from disk` → ชี้ไปที่โฟลเดอร์ที่เพิ่งโคลนมา

5. **เปิดโปรเจกต์ แล้วปล่อยให้มันทำงาน**
   จะขึ้นแถบ `Importing assets` วิ่งไปเรื่อยๆ **อย่าปิด อย่ากดอะไร** รอจนเข้าหน้า Editor ได้

6. **เปิดซีนหลัก** `Assets/project.unity` แล้วกด Play

### ถ้าเปิดมาแล้ว UI หรือระบบบางอย่างหาย

ไปที่ **Tools → Farming → ติดตั้งระบบ Farming** กดหนึ่งครั้ง แล้วเซฟซีน (`Ctrl+S`)

---

## แผนสำรองสำหรับวันพรีเซน

อย่าพึ่งทางเดียว ถ้าเน็ตล่ม เครื่องไม่มี Unity หรือโหลดไม่ทัน จะจบเลย

**ทำไฟล์เกมสำเร็จรูปติด USB ไปด้วย** — ใช้เวลาทำแค่ไม่กี่นาที ที่เครื่องคุณเอง

1. ใน Unity → `File → Build Profiles`
2. เลือก **Windows** → กด **Build**
3. เลือกโฟลเดอร์ว่างสักอัน เช่น `ExoticFarm_Build`
4. รอ build เสร็จ แล้วก๊อบทั้งโฟลเดอร์ลง USB

ที่งานแค่เสียบ USB ดับเบิลคลิกไฟล์ `.exe` ก็เล่นได้ทันที ไม่ต้องมี Unity ไม่ต้องมีเน็ต

> ถ้าอยากโชว์โค้ดด้วยก็ยังเปิดโปรเจกต์จริงได้ แต่มีไฟล์ build ติดตัวไว้อุ่นใจกว่าเยอะ

---

## ใช้งานประจำวัน

**ตอนทำงานเสร็จในแต่ละวัน** (เครื่องไหนก็ได้)

1. **ปิด Unity ก่อน** — สำคัญมาก กัน Unity เขียนไฟล์ทับตอนกำลังอัป
2. เปิด GitHub Desktop → เห็นรายการไฟล์ที่แก้ไป
3. พิมพ์สรุปสั้นๆ ว่าทำอะไร เช่น `เพิ่มโหมดเจ้าของเกม F9`
4. กด **Commit to main**
5. กด **Push origin** ที่แถบบน

**ตอนเริ่มทำงานที่อีกเครื่อง**

1. เปิด GitHub Desktop **ก่อนเปิด Unity**
2. กด **Fetch origin** แล้วกด **Pull origin** ถ้ามีของใหม่
3. ค่อยเปิด Unity

> กฎเดียวที่ต้องจำ: **push ก่อนลุกจากเครื่อง, pull ก่อนเริ่มทำงาน**
> ถ้าลืมแล้วไปแก้ไฟล์เดียวกันสองเครื่อง Git จะขึ้น conflict ซึ่งแก้ยุ่งยากมากในไฟล์ซีนของ Unity

---

## ปัญหาที่เจอบ่อย

**เปิดที่เครื่องใหม่แล้วซีนว่างเปล่า / ของหายหมด**
→ อาจเป็นเพราะไฟล์ `.meta` ไม่ได้ขึ้นไปด้วย ลองเช็กบน GitHub ว่ามีไฟล์ `.meta` อยู่ข้างๆ ไฟล์ภาพไหม

**เมนู Tools → Farming ไม่ขึ้น**
→ โค้ดยังคอมไพล์ไม่เสร็จ หรือมี error ดูที่หน้าต่าง Console

**UI หรือระบบบางอย่างหายไปในซีน**
→ ไปที่ **Tools → Farming → ติดตั้งระบบ Farming** กดหนึ่งครั้ง แล้วเซฟซีน (`Ctrl+S`)

**GitHub บอกว่าไฟล์ใหญ่เกิน 100 MB**
→ มีไฟล์ใหญ่หลุดเข้าไป ทักมาบอกได้ จะช่วยกรองออกให้

---

## ข้อควรรู้ถ้าตั้งเป็น Public

repo แบบ Public ใครก็เห็นได้ รวมถึงไฟล์อาร์ตในโฟลเดอร์ `Assets/Art`
อาร์ตบางส่วนมาจากชุดตัวอย่างของ Unity และแหล่งอื่น ซึ่งปกติอนุญาตให้ใช้ในงานของเราได้
แต่การเผยแพร่ไฟล์ต้นฉบับต่อสาธารณะเป็นคนละเรื่องกัน

ถ้าไม่แน่ใจ เปลี่ยนเป็น Private ได้ทีหลังที่
**GitHub → repo → Settings → เลื่อนลงล่างสุด → Change repository visibility**
